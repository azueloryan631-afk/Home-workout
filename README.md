# Home-workout
SYSTEM is a single-file HTML fitness RPG application (240KB, ~4,600 lines) that runs entirely in the browser with zero dependencies except Chart.js. All data is stored in localStorage — no backend, no accounts, no server. It can be installed as a PWA for offline use.
